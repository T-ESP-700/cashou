meta {
  name: Start Quiz
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/trpc/userQuiz.startQuiz
  body: json
  auth: inherit
}

body:json {
  {
    "quizId": 2,
    "userId": "usr_demo_1"
  }
}
