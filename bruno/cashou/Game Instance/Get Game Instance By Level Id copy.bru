meta {
  name: Get Game Instance By Level Id copy
  type: http
  seq: 10
}

get {
  url: {{base_url}}/trpc/gameInstance.getByUser?input={"userId":3}
  body: json
  auth: inherit
}

params:query {
  input: {"userId":3}
}

settings {
  encodeUrl: true
  timeout: 0
}
