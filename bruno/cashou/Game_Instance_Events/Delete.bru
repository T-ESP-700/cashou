meta {
  name: Delete
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/trpc/gameInstanceEvent.delete
  body: json
  auth: inherit
}

body:json {
  {
    "id": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
