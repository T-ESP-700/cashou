meta {
  name: Get All Notifications
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/trpc/notification.getAll
  body: json
  auth: inherit
}

body:json {
  {
  
  }
}

settings {
  encodeUrl: true
}
