meta {
  name: Get all event_assets for a specific event
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/trpc/eventAsset.getByEventId?input={"eventId":1}
  body: none
  auth: inherit
}

params:query {
  input: {"eventId":1}
}

settings {
  encodeUrl: true
}
