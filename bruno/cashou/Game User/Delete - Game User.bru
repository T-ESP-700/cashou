meta {
  name: Delete - Game User
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/trpc/gameUser.delete
  body: json
  auth: inherit
}

body:json {
  {
    "id": 9
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
