meta {
  name: Get Game User By Status - Game User
  type: http
  seq: 6
}

get {
  url: {{base_url}}/trpc/gameUser.findByStatus?input={"status":"inactive"}
  body: json
  auth: inherit
}

params:query {
  input: {"status":"inactive"}
}

settings {
  encodeUrl: true
  timeout: 0
}
