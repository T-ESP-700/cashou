meta {
  name: Get Game User By User - Game User
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/trpc/gameUser.findByUser?input={"userId":3}
  body: json
  auth: inherit
}

params:query {
  input: {"userId":3}
}

settings {
  encodeUrl: true
  timeout: 0
}
