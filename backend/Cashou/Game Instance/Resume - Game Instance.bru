meta {
  name: Resume - Game Instance
  type: http
  seq: 5
}

post {
  url: {{base_url}}/trpc/gameInstance.resume
  body: json
  auth: inherit
}

body:json {
  {
    "id": 3
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
