meta {
  name: Delete - Game Instance
  type: http
  seq: 8
}

post {
  url: {{base_url}}/trpc/gameInstance.delete?input={"id":5}
  body: json
  auth: inherit
}

params:query {
  input: {"id":5}
}

settings {
  encodeUrl: true
  timeout: 0
}
