meta {
  name: Update - Game User
  type: http
  seq: 8
}

post {
  url: {{base_url}}/trpc/gameUser.updateStatus
  body: json
  auth: inherit
}

body:json {
  {
    "id": 10,
    "status": "inactive"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
