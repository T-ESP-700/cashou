meta {
  name: Get Game User By Id - Game User
  type: http
  seq: 3
}

get {
  url: {{base_url}}/trpc/gameUser.findById?input={"id":5}
  body: json
  auth: inherit
}

params:query {
  input: {"id":5}
}

settings {
  encodeUrl: true
  timeout: 0
}
