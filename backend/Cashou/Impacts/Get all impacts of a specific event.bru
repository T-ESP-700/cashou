meta {
  name: Get all impacts of a specific event
  type: http
  seq: 4
}

get {
  url: {{base_url}}/trpc/impact.getByEventId?input={"eventId":1}
  body: none
  auth: inherit
}

params:query {
  input: {"eventId":1}
}

settings {
  encodeUrl: true
}
