meta {
  name: Shuffle Quiz Order
  type: http
  seq: 9
}

post {
  url: {{base_url}}/trpc/quizQuestion.shuffleQuizOrder
  body: json
  auth: inherit
}

body:json {
  {"quizId":1}
}

settings {
  encodeUrl: true
}
