meta {
  name: Delete - Wallets
  type: http
  seq: 4
}

post {
  url: {{base_url}}/trpc/wallet.delete
  body: json
  auth: inherit
}

body:json {
  {
    "id": 10
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
